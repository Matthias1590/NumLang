push(1)
if
    push("The if block will be executed if the top value on the stack is not 0.")
    write
end

push(0)
if
    // ...
else
    push("The else block will be executed if the top value on the stack is 0.")
    write
end